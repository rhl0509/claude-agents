---
description: Distill this session into atomic, evidence-backed learnings to save to E:\claude_memory (self-improvement ritual; Korean alias: 회고)
argument-hint: [scope hint (optional)]
---
Reflect on **this session** and distill learnings worth saving to `E:\claude_memory\` so the next session's me is better. This ports ECC continuous-learning's discipline (atomic, confidence-weighted, evidence-backed) onto the E: single-source memory system. **You are the main session, so you can see the whole conversation — this dialogue is the observation source.** Follow the same procedure as the `회고` command:

Scope hint: $ARGUMENTS

1. **Extract signals** (atomic, one fact each): user corrections → feedback; repeated preferences → user/feedback; mistake→fix paths → feedback; durable facts not derivable from code/git/existing memory/CLAUDE.md → project/reference/user.
2. **Noise filter (strict)**: drop anything already in E:\claude_memory / global or repo CLAUDE.md / git history, and anything only relevant to this conversation. If nothing survives, honestly report "no learnings to save" and stop — never manufacture.
3. **Format each survivor** as an E: file candidate (their frontmatter: name/description/metadata.type; feedback/project add **Why:**/**How to apply:**) plus `confidence` (0.3 tentative–0.9 certain), `evidence` (what in this session + date), and a date-index pointer line. Do not write yet.
4. **Confidence evolution**: if a candidate matches existing memory, propose *updating* it (add evidence, raise confidence) instead of a new file; if it contradicts existing memory, flag that memory for downgrade/revision.
5. **Present, then wait**: show candidates as a ranked table and stop. Only write to E: the items the user approves ("기록해"); on approval, write the file and add one pointer line to today's `YYYYMMDD_MEMORY.md`. If E: is unmounted, say so — never silently skip. The main session owns memory writes.

Output in Korean, ranked by confidence/impact.
